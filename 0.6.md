```mermaid
sequenceDiagram
    participant browser
    participant server

    Note over browser: The user writes a new note and presses the Save button.

    Note over browser: JavaScript code (spa.js) catches the submit event, stopping the default submit handler (e.preventDefault())
    Note over browser: JavaScript instantly adds new records to the local list and re-renders the screen view instantly

    browser->>server: POST [https://studies.cs.helsinki.fi/exampleapp/new_note_spa](https://studies.cs.helsinki.fi/exampleapp/new_note_spa)
    activate server
    Note over server: The server receives the JSON data and saves it to a database/array.
    server-->>browser: HTTP status code 201 (Created)
    deactivate server

    Note right of browser: The browser receives a successful response from the server without needing to reload the page or request data again.